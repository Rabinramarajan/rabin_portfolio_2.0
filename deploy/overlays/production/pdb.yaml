apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: portfolio
spec:
  # With minReplicas 2, this lets a node drain evict one pod at a time.
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: portfolio
