# dependencies & build output — rebuilt inside the image
node_modules
.next
out
build

# secrets — must never enter the build context
.env
.env.*
*.pem
*.key

# vcs & tooling
.git
.github
.vscode
.claude
.idea

# publish pipeline itself
publish

# docs, scratch files, archives
*.md
*.zip
*.log
coverage

# design/source assets not served at runtime
ux
animation
