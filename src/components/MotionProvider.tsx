"use client";

/**
 * Motion Provider — Disabled
 * All motion effects have been removed
 */

export function MotionProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
